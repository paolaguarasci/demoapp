spring:
  datasource:
    url: ${FLORENCE_APP_DB_URI}
    username: ${FLORENCE_APP_DB_USERNAME}
    password: ${FLORENCE_APP_DB_PASSWORD}
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  config:
    init:
      db: true
